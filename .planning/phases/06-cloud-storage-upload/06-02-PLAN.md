---
phase: 06-cloud-storage-upload
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/settings/CloudStorageSettingsForm.tsx
  - src/app/(app)/settings/page.tsx
  - src/components/schedule/ScheduleModal.tsx
  - src/app/(app)/history/HistoryPageClient.tsx
autonomous: true
requirements:
  - CLOD-02
  - CLOD-03

must_haves:
  truths:
    - "관리자가 /settings 페이지에서 Cloud Storage 탭을 볼 수 있다"
    - "Cloud Storage 탭에서 S3 연결 정보(엔드포인트, 리전, 버킷, 키)를 저장할 수 있다"
    - "secretAccessKey는 폼에서 마스킹되고 수정 가능하다"
    - "Test Connection 버튼이 HeadBucket 결과를 토스트로 표시한다"
    - "ScheduleModal에 클라우드 업로드 토글이 있다"
    - "글로벌 S3 설정이 없으면 토글이 비활성화되고 안내 문구가 표시된다"
    - "히스토리 테이블 Status 컬럼 옆에 cloudUploadStatus 아이콘이 표시된다"
  artifacts:
    - path: "src/components/settings/CloudStorageSettingsForm.tsx"
      provides: "S3 설정 폼 컴포넌트 (엔드포인트, 리전, 버킷, 키, 테스트 버튼)"
      exports: ["CloudStorageSettingsForm"]
    - path: "src/app/(app)/settings/page.tsx"
      provides: "Tabs 구조로 리팩터링된 설정 페이지 (Notifications | Cloud Storage)"
    - path: "src/components/schedule/ScheduleModal.tsx"
      provides: "cloudUpload 토글이 추가된 스케줄 모달"
    - path: "src/app/(app)/history/HistoryPageClient.tsx"
      provides: "cloudUploadStatus 아이콘이 추가된 히스토리 테이블"
  key_links:
    - from: "src/components/settings/CloudStorageSettingsForm.tsx"
      to: "/api/cloud-storage/settings"
      via: "fetch GET on mount, PUT on save"
      pattern: "fetch.*cloud-storage/settings"
    - from: "src/components/settings/CloudStorageSettingsForm.tsx"
      to: "/api/cloud-storage/test"
      via: "fetch POST on test button click"
      pattern: "fetch.*cloud-storage/test"
    - from: "src/components/schedule/ScheduleModal.tsx"
      to: "/api/cloud-storage/settings"
      via: "fetch GET to check if settings exist (disable toggle if not)"
      pattern: "fetch.*cloud-storage/settings"
---

<objective>
클라우드 스토리지 설정 UI, 스케줄별 업로드 토글, 히스토리 아이콘을 구현한다. 06-01이 완성한 API 위에 모든 사용자 인터페이스를 올린다.

Purpose: 사용자가 S3 연결을 설정하고, 스케줄별로 업로드를 켜고 끄고, 히스토리에서 업로드 결과를 확인할 수 있게 한다.
Output: CloudStorageSettingsForm 컴포넌트, 탭 리팩터링된 settings/page.tsx, ScheduleModal 수정, HistoryPageClient 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cloud-storage-upload/06-CONTEXT.md
@.planning/phases/06-cloud-storage-upload/06-01-SUMMARY.md

<interfaces>
<!-- 이 플랜의 executor가 필요한 기존 패턴 -->

NotificationSettingsForm 패턴 (src/components/settings/NotificationSettingsForm.tsx):
```typescript
"use client"
// useState for each field, loading/saving/testing states
// useEffect on mount: fetch("/api/settings/notifications").then(({ data }) => populate state)
// handleSave(): fetch PUT, toast.success / toast.error
// handleTest(): fetch POST to test endpoint
// Masked secret pattern: passwordSet boolean + placeholder="저장된 비밀번호 유지"
// Returns JSX with shadcn Card + CardContent + Label + Input + Switch + Button
```

settings/page.tsx current structure:
```typescript
import { NotificationSettingsForm } from "@/components/settings/NotificationSettingsForm"
// Renders: <h1>설정</h1> + <NotificationSettingsForm />
// Must become: <Tabs> with Notifications tab + Cloud Storage tab
```

NotificationSettingsForm already uses Tabs internally (email/slack sub-tabs).
settings/page.tsx needs outer Tabs: "notifications" | "cloud-storage"
Import: Tabs, TabsList, TabsTrigger, TabsContent from "@/components/ui/tabs"

ScheduleModal (src/components/schedule/ScheduleModal.tsx):
```typescript
// ScheduleData type currently has: id, connectionId, isEnabled, hour, minute,
//   backupPath, retentionDays, notificationsEnabled, catchUpOnRestart
// Must add: cloudUpload: boolean
// State: const [cloudUpload, setCloudUpload] = useState(false)
// useEffect populate: setCloudUpload(schedule.cloudUpload ?? false)
// payload: add cloudUpload field
// UI: Section 6 after existing Section 5 (알림)
```

HistoryPageClient (src/app/(app)/history/HistoryPageClient.tsx):
```typescript
// HistoryItem interface currently has: id, connectionId, connectionName, dbType,
//   status, fileName, fileSizeBytes, durationMs, startedAt, completedAt
// Must add: cloudUploadStatus: string | null
// TableCell for StatusBadge already exists at line ~310
// Add cloud icon inline after <StatusBadge> in same TableCell
// Lucide icon: Cloud (with CheckCircle or XCircle overlay) OR simple text
// API response at /api/backups must return cloudUploadStatus (check route.ts)
```

API routes created in 06-01:
- GET /api/cloud-storage/settings → { data: { endpoint, region, bucket, accessKeyId, secretAccessKey: "__masked__" | null, ...} | null }
- PUT /api/cloud-storage/settings → { data: { saved: true } }
- POST /api/cloud-storage/test → { data: { success: boolean, message: string } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CloudStorageSettingsForm + refactor settings/page.tsx to Tabs</name>
  <files>src/components/settings/CloudStorageSettingsForm.tsx, src/app/(app)/settings/page.tsx</files>
  <action>
**`src/components/settings/CloudStorageSettingsForm.tsx`** — follow NotificationSettingsForm.tsx structure exactly.

```typescript
"use client"
import { useState, useEffect } from "react"
import { Card, CardContent } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Button } from "@/components/ui/button"
import { Loader2 } from "lucide-react"
import { toast } from "sonner"

export function CloudStorageSettingsForm() { ... }
```

State variables:
- `endpoint` (string), `region` (string), `bucket` (string), `accessKeyId` (string)
- `secretAccessKey` (string) — input field value, cleared after save
- `secretAccessKeySet` (boolean) — true when server has a saved encrypted value (from `"__masked__"`)
- `loading` (boolean, true initially), `saving` (boolean), `testing` (boolean)

**Load on mount (`useEffect`):**
```typescript
fetch("/api/cloud-storage/settings")
  .then((res) => res.json())
  .then(({ data }) => {
    if (data) {
      setEndpoint(data.endpoint ?? "")
      setRegion(data.region ?? "")
      setBucket(data.bucket ?? "")
      setAccessKeyId(data.accessKeyId ?? "")
      setSecretAccessKeySet(data.secretAccessKey === "__masked__")
    }
  })
  .catch(() => toast.error("설정을 불러오는 데 실패했습니다"))
  .finally(() => setLoading(false))
```

**handleSave():**
```typescript
setSaving(true)
try {
  const res = await fetch("/api/cloud-storage/settings", {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      endpoint: endpoint || null,
      region: region || null,
      bucket: bucket || null,
      accessKeyId: accessKeyId || null,
      secretAccessKey: secretAccessKey || null,  // empty = preserve existing
    }),
  })
  if (!res.ok) { const { error } = await res.json(); throw new Error(error ?? "저장 실패") }
  toast.success("클라우드 스토리지 설정이 저장되었습니다")
  setSecretAccessKeySet(!!secretAccessKey || secretAccessKeySet)
  setSecretAccessKey("")  // clear after save
} catch (err) {
  toast.error(`설정 저장에 실패했습니다: ${(err as Error).message}`)
} finally { setSaving(false) }
```

**handleTest():**
```typescript
setTesting(true)
try {
  const res = await fetch("/api/cloud-storage/test", { method: "POST" })
  const { data } = await res.json()
  if (data?.success) {
    toast.success(`연결 성공: ${data.message}`)
  } else {
    toast.error(`연결 실패: ${data?.message ?? "알 수 없는 오류"}`)
  }
} catch { toast.error("연결 테스트에 실패했습니다") }
finally { setTesting(false) }
```

**Render (inside Card > CardContent className="p-6 space-y-4"):**
- Section header: `<div className="text-sm font-semibold text-neutral-900">S3 호환 스토리지 설정</div>`
- Endpoint field: Label "엔드포인트 (선택)", Input placeholder="https://s3.amazonaws.com (비워두면 AWS 기본값)"
- Region field: Label "리전", Input placeholder="ap-northeast-2"
- Bucket field: Label "버킷 이름", Input placeholder="my-backup-bucket"
- Access Key ID field: Label "Access Key ID", Input
- Secret Access Key field: Label "Secret Access Key", Input type="password", placeholder={secretAccessKeySet ? "저장됨 (변경하려면 새 키 입력)" : "Secret Access Key 입력"}
- Helper text: `<p className="text-xs text-neutral-400">AWS IAM 또는 호환 서비스의 자격 증명을 입력하세요</p>`
- Buttons row (flex justify-end gap-3 pt-4):
  - Test Connection button (variant="outline", onClick=handleTest, disabled=testing): Loader2 spinner when testing + "연결 테스트"
  - Save button (onClick=handleSave, disabled=saving): Loader2 spinner when saving + "저장"

Loading state: return `<div className="flex justify-center py-12"><Loader2 className="animate-spin" /></div>` while loading.

── ── ──

**`src/app/(app)/settings/page.tsx`** — add outer Tabs (D-09):

```typescript
import { auth } from "@/auth"
import { redirect } from "next/navigation"
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs"
import { NotificationSettingsForm } from "@/components/settings/NotificationSettingsForm"
import { CloudStorageSettingsForm } from "@/components/settings/CloudStorageSettingsForm"

export default async function SettingsPage() {
  const session = await auth()
  if (!session?.user) redirect("/login")
  if (session.user.role !== "admin") redirect("/connections")

  return (
    <div className="max-w-2xl mx-auto">
      <h1 className="text-xl font-semibold text-neutral-900 mb-6">설정</h1>
      <Tabs defaultValue="notifications">
        <TabsList className="mb-6">
          <TabsTrigger value="notifications">알림</TabsTrigger>
          <TabsTrigger value="cloud-storage">클라우드 스토리지</TabsTrigger>
        </TabsList>
        <TabsContent value="notifications">
          <NotificationSettingsForm />
        </TabsContent>
        <TabsContent value="cloud-storage">
          <CloudStorageSettingsForm />
        </TabsContent>
      </Tabs>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>CloudStorageSettingsForm.tsx created with load/save/test handlers. settings/page.tsx wrapped with Tabs showing Notifications and Cloud Storage tabs. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add cloudUpload toggle to ScheduleModal + cloud icon to history table</name>
  <files>src/components/schedule/ScheduleModal.tsx, src/app/(app)/history/HistoryPageClient.tsx</files>
  <action>
**`src/components/schedule/ScheduleModal.tsx`** — add cloudUpload toggle (D-10):

1. **Update `ScheduleData` type** — add `cloudUpload: boolean` field:
   ```typescript
   export type ScheduleData = {
     // ...existing fields unchanged...
     cloudUpload: boolean  // add this
   }
   ```

2. **Add state variables:**
   ```typescript
   const [cloudUpload, setCloudUpload] = useState(false)
   const [cloudStorageConfigured, setCloudStorageConfigured] = useState(false)
   ```

3. **In the mount useEffect** (where schedule fields are populated), add:
   ```typescript
   setCloudUpload(schedule?.cloudUpload ?? false)
   ```
   Also check if S3 is configured on open:
   ```typescript
   // Check if cloud storage is configured to control toggle availability
   fetch("/api/cloud-storage/settings")
     .then((res) => res.json())
     .then(({ data }) => {
       setCloudStorageConfigured(!!(data?.bucket && data?.accessKeyId && data?.secretAccessKey))
     })
     .catch(() => {})
   ```
   Place this fetch inside the `if (open)` block, outside the `if (schedule)` conditional.

4. **Add `cloudUpload` to the save payload:**
   ```typescript
   const payload = {
     // ...existing fields...
     cloudUpload,
   }
   ```

5. **Add Section 6 UI** after the existing notifications section (after Section 5 closing `</div>`):
   ```tsx
   {/* Section 6: 클라우드 업로드 */}
   <div className="space-y-2">
     <div className="flex items-center gap-3">
       <Switch
         checked={cloudUpload}
         onCheckedChange={setCloudUpload}
         disabled={!cloudStorageConfigured}
         aria-label="백업 파일 클라우드 업로드"
       />
       <Label className={!cloudStorageConfigured ? "text-neutral-400" : ""}>
         백업 완료 후 클라우드 업로드
       </Label>
     </div>
     {!cloudStorageConfigured && (
       <p className="text-xs text-neutral-400">
         클라우드 스토리지 설정이 필요합니다.{" "}
         <a href="/settings?tab=cloud-storage" className="text-indigo-600 underline">
           설정에서 구성하세요
         </a>
       </p>
     )}
     {cloudStorageConfigured && (
       <p className="text-xs text-neutral-400">
         백업 성공 시 S3 호환 스토리지에 자동 업로드됩니다
       </p>
     )}
   </div>
   ```

── ── ──

**`src/app/(app)/history/HistoryPageClient.tsx`** — add cloudUploadStatus (D-03):

1. **Update `HistoryItem` interface** — add field:
   ```typescript
   interface HistoryItem {
     // ...existing fields unchanged...
     cloudUploadStatus: string | null  // add this
   }
   ```

2. **Add import** at top of file:
   ```typescript
   import { Cloud, CloudOff } from "lucide-react"
   ```

3. **Add `CloudUploadIcon` helper component** near `StatusBadge`:
   ```tsx
   function CloudUploadIcon({ status }: { status: string | null }) {
     if (!status || status === "skipped") return null
     if (status === "success") {
       return (
         <span title="S3 업로드 성공" className="inline-flex items-center ml-1.5 text-blue-500">
           <Cloud className="h-3.5 w-3.5" />
         </span>
       )
     }
     // failed
     return (
       <span title="S3 업로드 실패" className="inline-flex items-center ml-1.5 text-red-400">
         <CloudOff className="h-3.5 w-3.5" />
       </span>
     )
   }
   ```

4. **Update the Status TableCell** — find the cell that renders `<StatusBadge status={item.status} />` (around line 310) and add the icon inline:
   ```tsx
   <TableCell>
     <div className="flex items-center">
       <StatusBadge status={item.status} />
       <CloudUploadIcon status={item.cloudUploadStatus} />
     </div>
   </TableCell>
   ```

5. **Check /api/backups route** — open `src/app/api/backups/route.ts` and verify `cloudUploadStatus` is included in the select/return fields. If the Prisma query uses `select` and omits `cloudUploadStatus`, add it. If it uses `findMany` without a `select` (returns all fields), no change needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ScheduleModal.tsx has cloudUpload state, checks S3 configured on open, disables toggle with guidance if not configured, sends cloudUpload in payload. HistoryPageClient.tsx has cloudUploadStatus in HistoryItem, CloudUploadIcon renders Cloud/CloudOff based on status. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — zero errors
2. `/settings` page renders two tabs: 알림 and 클라우드 스토리지
3. CloudStorageSettingsForm loads from GET /api/cloud-storage/settings and shows masked placeholder when secretAccessKey is set
4. ScheduleModal has cloudUpload switch in Section 6
5. History table Status column shows Cloud/CloudOff icon when cloudUploadStatus is non-null
6. Test Connection button in CloudStorageSettingsForm calls POST /api/cloud-storage/test and shows toast
</verification>

<success_criteria>
- /settings page has outer Tabs: 알림 (NotificationSettingsForm) | 클라우드 스토리지 (CloudStorageSettingsForm) — per D-09
- CloudStorageSettingsForm: secretAccessKey field shows "저장됨" placeholder when server value exists — per D-08
- ScheduleModal cloudUpload toggle disabled + guidance text when no S3 settings exist — per D-10
- History table shows ☁ success / ☁ fail icons in Status column — per D-03
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-cloud-storage-upload/06-02-SUMMARY.md`
</output>
