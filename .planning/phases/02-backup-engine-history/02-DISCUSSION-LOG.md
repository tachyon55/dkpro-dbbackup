# Phase 2: Backup Engine + History - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 02-backup-engine-history
**Areas discussed:** 백업 실행 흐름, 실시간 진행 표시, 히스토리 조회 UI, 파일 다운로드 & 무결성 검증

---

## 백업 실�� 흐름

### 백업 트리거 위치

| Option | Description | Selected |
|--------|-------------|----------|
| 연결 카드에서 직접 (추천) | 기존 연결 카드에 "백업 실행" 버튼 추가. 카드 선택 → 백업 실행 흐름이 간결함 | ✓ |
| 별��� 백업 페이지 | 사이드바에 "백업" 메뉴 추가, 연결 선택 → 백업 실행. 전용 페이지로 백업 관리 중앙화 | |
| 모두 (카드 + 전용 페이지) | 카드에서도 바로 실행 가능하고, 전용 페이지에서 여러 연결 일괄 백업도 가능 | |

**User's choice:** 연결 카드에서 직접 (추천)
**Notes:** —

### 백업 실행 전 옵션

| Option | Description | Selected |
|--------|-------------|----------|
| 즉시 실행 (추천) | 버튼 클릭 → ���인 다이얼로그 → 바로 실행. 저장 경로는 시스템 기본값 사용 | ✓ |
| 간단 옵션 모달 | 저장 경로, 압축 여부 등 간단한 옵션을 모달에서 선택 후 실행 | |
| 확인만 (옵션 없이) | "이 연결을 백업하시겠습니까?" 확인만 표시, 옵션 없음 | |

**User's choice:** 즉시 실행 (추천)
**Notes:** —

### 동시 백업 차단 UI

| Option | Description | Selected |
|--------|-------------|----------|
| 버튼 비활성화 (추천) | 백업 중인 연결의 백업 버튼을 비활성화하고 "백업 중..." 상��� 표시 | ✓ |
| 알림 메시지 | 버튼은 활성화 상태로 두되, 클릭 시 "이미 백업 중입니다" 토스트 표시 | |
| You decide | Claude가 적절한 방식을 선택 | |

**User's choice:** 버튼 비���성화 (추천)
**Notes:** —

---

## 실시간 진행 표시

### 통신 방식

| Option | Description | Selected |
|--------|-------------|----------|
| SSE (Server-Sent Events) (추천) | 단방향 스트림으로 충분. Next.js API Route에서 바로 사용 가능, 별도 서버 불필요 | ✓ |
| Socket.io (WebSocket) | PROJECT.md에 Socket.io 명시. 양방향 통신 가능하지만 커스텀 서버 필요 | |
| You decide | Claude가 기술적으로 적절한 방식 선택 | |

**User's choice:** SSE (Server-Sent Events) (추천)
**Notes:** PROJECT.md��� Socket.io가 명시되어 있으나, 백업 진행은 서버→클라이언트 단방향이므로 SSE가 더 적합

### 진행 UI 형태

| Option | Description | Selected |
|--------|-------------|----------|
| 로��� 스트림 + 프로그레스 (추천) | dump 도구의 stdout/stderr를 로그로 스트리밍하고, 상단에 프로그레스 바 표시 | ✓ |
| 로그 스트림만 | 터미널 스타일로 dump 도구 출력을 실시간 표시 | |
| 프로그레스 바만 | 간단한 프로그레스 바 + 상태 텍스트 | |

**User's choice:** 로그 스트림 + 프로그레스 (추천)
**Notes:** —

### 표시 위치

| Option | Description | Selected |
|--------|-------------|----------|
| 전용 모달/드로어 (추천) | 백업 실행 버튼 클릭 시 모달/드로어가 열리고 로그+프로그레스 표시. ��료 후 닫기 가능 | ✓ |
| 카드 인라인 | 연결 카드 자체에 미니 진행 바 + 상태 표시 (로그는 클릭하면 확장) | |
| 하단 패널 | 화면 하단에 고정된 백업 진행 패널 (IDE 터미널 스타일) | |

**User's choice:** 전용 모달/드로어 (추천)
**Notes:** —

### 완료 액션

| Option | Description | Selected |
|--------|-------------|----------|
| 결과 요약 + 닫기 (추천) | 성공/실패 상태, 파일 크기, 소요시간 요약 표시. 닫기 버튼으로 돌아감 | ✓ |
| 결과 + 즉시 다운로드 | 성공 시 파일 다운로드 버튼도 함께 표시 | |
| You decide | Claude가 적절한 UX 설계 | |

**User's choice:** 결과 요약 + 닫기 (추천)
**Notes:** —

---

## 히스��리 조회 UI

### 조회 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 전체 목록 + 필터 (추천) | 모든 연결의 백업 이력을 한 테이블에 표시. 연결별/상태별/날짜 필터로 좁혀보기 | �� |
| 연결별 히스토리 | 연결 카드 클릭 → 해당 연결의 백업 이력만 표시 | |
| 모두 (전체 + 연결별) | 사이드바에 "백업 히스토리" 메뉴(전체 목록) + 연결 카드에서도 해당 연결 이력 조회 가��� | |

**User's choice:** 전체 목록 + 필터 (추천)
**Notes:** —

### 테이블 컬럼

| Option | Description | Selected |
|--------|-------------|----------|
| 핵심 정보 (추천) | 연결명, DB타입, 상태(성공/실패/진행중), 파��명, 파일크기, ���요시간, ��행일시 | ✓ |
| 최소 정보 | 연결명, 상태, 실행일시만 표시 (간결) | |
| You decide | Claude가 HIST-01~04 요구사항 기반으로 적절히 구성 | |

**User's choice:** 핵심 정보 (추천)
**Notes:** ���

### 상세 로그

| Option | Description | Selected |
|--------|-------------|----------|
| 행 클릭 → 상세 패널 (추천) | 테이블 행 클릭 시 오른쪽이나 하단에 상세 패널이 열리며 dump 로그 전문, 에러 메시지 등 표시 | ✓ |
| 모달 다이얼로그 | 테이블 행 클릭 시 모달로 로그 상세 표시 | |
| You decide | Claude가 적절한 패턴 선택 | |

**User's choice:** 행 클릭 → 상세 패널 (추천)
**Notes:** —

---

## 파일 다운로드 & 무결성 검증

### 다운로드 위치

| Option | Description | Selected |
|--------|-------------|----------|
| 히스토리 상세에서 (추천) | 히스토리 테이블 행 클릭 → 상세 패널에서 다운로드 버튼 제공. 파일 정보 확인 후 ���운로드 | ✓ |
| 테이블에 직접 버튼 | 히스토��� 테이블 각 행에 다운로드 아이콘 버튼 바로 표시 | |
| 모두 (상세 + 테이블) | 테이블에 빠른 다운로드 아이콘 + 상세에 풀 다운로드 버튼 | |

**User's choice:** 히��토리 상세에서 (추천)
**Notes:** ��

### SHA-256 해시 검증

| Option | Description | Selected |
|--------|-------------|----------|
| 해시값 표시 + 복사 (추천) | 상세 패널에 SHA-256 해시값 표시, 클립보드 복사 버튼 제공. 사용자가 다운로드 후 로컬에서 비교 | ✓ |
| 자동 검증 버튼 | "무결성 검증" 버튼 클릭 → 서버에서 파일 해시 재계산 → DB 저장 해시와 비교 → 일치/불일치 표시 | |
| You decide | Claude가 적절한 방식 선택 | |

**User's choice:** 해시값 표시 + 복사 (추���)
**Notes:** —

---

## Claude's Discretion

- 백업 파일 저장 디렉토리 구조
- 백업 파일명 포맷 상세
- SSE 이벤트 프로토콜 상세
- 히스토리 테이블 페이지네이션 방식
- 상세 패널 레이아웃과 UX 디테일
- Prisma 모델 설계
- AuditEventType 확장

## Deferred Ideas

None — discussion stayed within phase scope
