---
phase: quick
plan: 260406-mp8
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/validations/connection.ts
  - src/components/connections/ConnectionModal.tsx
  - src/components/connections/ConnectionCard.tsx
  - src/components/connections/ConnectionDetail.tsx
  - src/app/api/connections/route.ts
  - src/app/api/connections/[id]/route.ts
  - src/components/schedule/ScheduleModal.tsx
  - src/app/api/backups/[jobId]/stream/route.ts
  - src/lib/backup-engine.ts
  - src/lib/scheduler.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Connection 추가/수정 시 백업 저장 방식(로컬/클라우드)을 선택할 수 있다"
    - "로컬 선택 시 경로 입력 필드가 표시되고, 클라우드 선택 시 경로 입력이 불필요하다"
    - "스케줄 모달에서 백업 저장 경로는 Connection 설정값을 읽기 전용으로 표시한다"
    - "수동 백업 실행 시 Connection에 설정된 저장 경로를 사용한다"
    - "스케줄 백업 실행 시 Connection에 설정된 저장 경로를 사용한다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "DbConnection에 backupStorageType, backupLocalPath 필드 추가"
      contains: "backupStorageType"
    - path: "src/components/connections/ConnectionModal.tsx"
      provides: "백업 저장 방식 선택 UI (로컬/클라우드 라디오 + 경로 입력)"
    - path: "src/components/schedule/ScheduleModal.tsx"
      provides: "Connection 저장 설정을 읽기 전용 표시, 기존 backupPath 입력 제거"
  key_links:
    - from: "ConnectionModal.tsx"
      to: "/api/connections"
      via: "POST/PUT with backupStorageType + backupLocalPath"
      pattern: "backupStorageType.*backupLocalPath"
    - from: "backup-engine.ts"
      to: "DbConnection.backupLocalPath"
      via: "connection 조회 후 backupLocalPath를 outputDir로 사용"
      pattern: "backupLocalPath|backupStorageType"
---

<objective>
Connection에 백업 저장 경로(로컬/클라우드) 설정 필드를 추가하고, 스케줄과 수동 백업 실행 시 Connection의 설정을 사용하도록 변경한다.

Purpose: 백업 저장 위치를 Connection 단위로 관리하여, 스케줄에서 중복 설정하지 않고 일관된 저장 경로를 사용한다.
Output: Connection 모달에 저장 방식 선택 UI, 스케줄 모달에서 읽기 전용 표시, 백업 실행 시 Connection 설정 사용
</objective>

<context>
@prisma/schema.prisma
@src/lib/validations/connection.ts
@src/components/connections/ConnectionModal.tsx
@src/components/connections/ConnectionCard.tsx
@src/components/connections/ConnectionDetail.tsx
@src/app/api/connections/route.ts
@src/app/api/connections/[id]/route.ts
@src/components/schedule/ScheduleModal.tsx
@src/app/api/backups/[jobId]/stream/route.ts
@src/lib/backup-engine.ts
@src/lib/scheduler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 스키마 + 유효성 검증 + API에 백업 저장 필드 추가</name>
  <files>
    prisma/schema.prisma,
    src/lib/validations/connection.ts,
    src/app/api/connections/route.ts,
    src/app/api/connections/[id]/route.ts
  </files>
  <action>
1. **prisma/schema.prisma** — DbConnection 모델에 2개 필드 추가:
   - `backupStorageType String @default("local")` — "local" 또는 "cloud" 값. Prisma enum으로 만들지 말고 String으로 처리한다 (마이그레이션 단순화).
   - `backupLocalPath String?` — 로컬 저장 시 사용할 경로. null이면 기본 경로(getBackupDir) 사용.
   이 필드들을 `toolPath` 아래에 배치한다.

2. **Prisma 마이그레이션 실행:**
   ```bash
   npx prisma migrate dev --name add-backup-storage-to-connection
   ```

3. **src/lib/validations/connection.ts** — baseFields에 추가:
   - `backupStorageType: z.enum(["local", "cloud"]).default("local").optional()`
   - `backupLocalPath: z.string().nullable().optional()`
   - superRefine에 조건 추가: `backupStorageType === "local"` 이고 `backupLocalPath`가 비어있으면 허용 (null = 기본 경로 사용). 별도 필수 검증은 하지 않는다.

4. **src/app/api/connections/route.ts** — GET select에 `backupStorageType: true, backupLocalPath: true` 추가. POST create data에 `backupStorageType: data.backupStorageType ?? "local", backupLocalPath: data.backupLocalPath ?? null` 추가.

5. **src/app/api/connections/[id]/route.ts** — CONNECTION_SELECT에 `backupStorageType: true, backupLocalPath: true` 추가. PUT updateData 블록에 `backupStorageType`, `backupLocalPath` 처리 추가 (기존 패턴과 동일하게 `if (data.X !== undefined) updateData.X = data.X`).
  </action>
  <verify>
    <automated>npx prisma migrate status && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DbConnection에 backupStorageType/backupLocalPath 필드가 추가되고, API에서 CRUD 가능하며 TypeScript 컴파일 에러 없음</done>
</task>

<task type="auto">
  <name>Task 2: Connection 모달/카드/상세 UI + 스케줄 모달 읽기 전용 표시</name>
  <files>
    src/components/connections/ConnectionModal.tsx,
    src/components/connections/ConnectionCard.tsx,
    src/components/connections/ConnectionDetail.tsx,
    src/components/schedule/ScheduleModal.tsx
  </files>
  <action>
1. **ConnectionCard.tsx** — Connection 타입에 필드 추가:
   - `backupStorageType: string` (기본값 "local")
   - `backupLocalPath: string | null`

2. **ConnectionModal.tsx**:
   - modalSchema에 `backupStorageType: z.enum(["local", "cloud"]).default("local")` 과 `backupLocalPath: z.string().optional()` 추가.
   - defaultValues에 `backupStorageType: "local"`, `backupLocalPath: ""` 추가.
   - isEdit useEffect의 form.reset에 `backupStorageType: connection.backupStorageType ?? "local"`, `backupLocalPath: connection.backupLocalPath ?? ""` 추가.
   - !isEdit useEffect의 form.reset에도 동일 기본값 추가.
   - "백업 도구 경로" 섹션 아래에 **"백업 저장 설정"** 섹션(Section 4) 추가 (기존 Section 4 "연결 테스트"는 Section 5로):
     ```
     <h3>백업 저장 설정</h3>
     ```
     - 라디오 버튼 2개 (또는 Select): "로컬 저장" / "클라우드 저장"
     - `form.watch("backupStorageType")` 로 분기:
       - "local" 선택 시: 경로 입력 Input 표시. placeholder="기본 경로 사용 (비워두면 자동)". `form.register("backupLocalPath")` 사용.
       - "cloud" 선택 시: 안내 텍스트 "클라우드 스토리지 설정에 따라 자동 업로드됩니다" 표시. 경로 Input 숨김.
   - handleSubmit의 payload에 `backupStorageType: data.backupStorageType ?? "local"`, `backupLocalPath: data.backupStorageType === "local" ? (data.backupLocalPath || null) : null` 추가.

3. **ConnectionDetail.tsx** — 정보 탭의 "백업 도구 경로" Field 아래에:
   - `<Field label="백업 저장" value={connection.backupStorageType === "cloud" ? "클라우드" : "로컬"} />`
   - backupStorageType === "local" 이고 backupLocalPath가 있으면: `<Field label="백업 경로" value={connection.backupLocalPath} />`
   - backupStorageType === "local" 이고 backupLocalPath가 없으면: `<Field label="백업 경로" value="기본 경로" />`

4. **ScheduleModal.tsx**:
   - Props에 `backupStorageType?: string`, `backupLocalPath?: string | null` 추가.
   - 기존 "백업 저장 경로" 섹션(Section 2)을 **읽기 전용**으로 변경:
     - 기존 Input + setBackupPath 상태를 제거한다.
     - 대신 읽기 전용 표시:
       ```tsx
       <div className="space-y-2">
         <Label>백업 저장 설정</Label>
         <div className="px-3 py-2 bg-neutral-50 rounded-md border text-sm text-neutral-700">
           {backupStorageType === "cloud" ? "클라우드 저장" : backupLocalPath ? backupLocalPath : "기본 경로"}
         </div>
         <p className="text-xs text-neutral-400">
           백업 저장 경로는 연결 설정에서 변경할 수 있습니다
         </p>
       </div>
       ```
   - handleSave의 payload에서 `backupPath` 필드를 제거한다 (더 이상 스케줄에서 경로를 저장하지 않음).

5. **ConnectionsPageClient.tsx에서 ScheduleModal 호출부** — ScheduleModal에 `backupStorageType`과 `backupLocalPath` props 전달 필요. ConnectionsPageClient.tsx에서 ScheduleModal을 렌더할 때 `scheduleTarget` connection 객체에서 값을 읽어 전달:
   ```tsx
   backupStorageType={scheduleTarget?.backupStorageType ?? "local"}
   backupLocalPath={scheduleTarget?.backupLocalPath ?? null}
   ```
   파일: `src/app/(app)/connections/ConnectionsPageClient.tsx` 도 수정 대상에 포함.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Connection 모달에 백업 저장 방식 선택 UI가 표시되고, 스케줄 모달에서 읽기 전용으로 Connection의 저장 설정이 표시되며, TypeScript 에러 없음</done>
</task>

<task type="auto">
  <name>Task 3: 백업 실행 시 Connection 저장 설정 사용</name>
  <files>
    src/app/api/backups/[jobId]/stream/route.ts,
    src/lib/backup-engine.ts,
    src/lib/scheduler.ts
  </files>
  <action>
1. **src/lib/backup-engine.ts** — runBackup 함수 수정:
   - 현재 line 58에서 DbConnection을 findUnique로 가져온다. 이미 `conn` 객체에 `backupStorageType`, `backupLocalPath`가 포함된다 (Prisma 스키마에 추가했으므로).
   - line 95의 resolvedBackupDir 로직을 변경:
     ```typescript
     // 우선순위: 1) caller의 backupDir 인자 2) connection의 backupLocalPath 3) 기본 getBackupDir
     let resolvedBackupDir: string
     if (backupDir) {
       resolvedBackupDir = backupDir
     } else if (conn.backupStorageType === "local" && conn.backupLocalPath) {
       resolvedBackupDir = conn.backupLocalPath
       await mkdir(resolvedBackupDir, { recursive: true })
     } else {
       resolvedBackupDir = await getBackupDir(connectionId)
     }
     ```
   - `mkdir`가 import되어 있지 않다면 `import { mkdir } from "fs/promises"` 추가.
   - 클라우드 저장(conn.backupStorageType === "cloud") 시에도 일단 로컬에 임시 덤프 후 업로드하는 기존 패턴을 유지한다. 클라우드 업로드는 기존 scheduler.ts의 cloudUpload 로직이 이미 처리하므로, backupStorageType이 "cloud"인 경우에는 기본 getBackupDir로 로컬 임시 저장한다 (위 로직에서 else 분기에 해당).

2. **src/app/api/backups/[jobId]/stream/route.ts** — 수동 백업 시에도 Connection의 저장 설정을 사용하도록:
   - 현재 `await runBackup(jobId, send)` 호출에서 backupDir를 전달하지 않는다.
   - runBackup 내부에서 conn 객체를 이미 조회하므로, 별도 수정 불필요. Task 3의 backup-engine.ts 수정으로 자동 적용된다.
   - 단, 클라우드 저장 타입의 connection에서 수동 백업 시에도 클라우드 업로드를 수행해야 한다. backup-engine.ts의 runBackup 끝부분에서 cloudUpload 처리 로직을 확인한다:

3. **src/lib/scheduler.ts** — 스케줄 백업에서 backupDir 결정 로직 수정:
   - 현재 line 84: `const backupDir = schedule.backupPath ? schedule.backupPath : await getBackupDir(connectionId)`
   - 이를 Connection 우선으로 변경:
     ```typescript
     // Connection의 저장 설정 우선 사용
     const conn = await prisma.dbConnection.findUnique({ where: { id: connectionId } })
     let backupDir: string
     if (conn?.backupStorageType === "local" && conn.backupLocalPath) {
       backupDir = conn.backupLocalPath
       await mkdir(backupDir, { recursive: true })
     } else {
       backupDir = await getBackupDir(connectionId)
     }
     ```
   - `import { mkdir } from "fs/promises"` 가 없다면 추가.
   - 기존 `schedule.backupPath` 참조를 제거한다 (더 이상 스케줄에 경로를 저장하지 않으므로).
   - 클라우드 업로드 분기: 기존 `schedule.cloudUpload` 체크 외에, `conn?.backupStorageType === "cloud"` 일 때도 클라우드 업로드를 수행하도록 조건을 확장:
     ```typescript
     const shouldCloudUpload = schedule.cloudUpload || conn?.backupStorageType === "cloud"
     if (shouldCloudUpload) { ... }
     ```

4. **수동 백업의 클라우드 업로드** — 현재 수동 백업(stream route)에서는 클라우드 업로드가 수행되지 않는다. backup-engine.ts의 runBackup 끝부분에서 성공 시, connection의 backupStorageType이 "cloud"이면 클라우드 업로드를 수행하는 로직을 추가한다. 기존 scheduler.ts의 클라우드 업로드 코드를 참고하여 동일 패턴 적용:
   - `import { uploadToCloud } from "@/lib/cloud-storage"` (기존 모듈 확인 후 올바른 경로 사용)
   - runBackup의 성공 분기에서:
     ```typescript
     if (conn.backupStorageType === "cloud") {
       try {
         await uploadToCloud(outputPath)
         await prisma.backupHistory.update({
           where: { id: historyId },
           data: { cloudUploadStatus: "success" },
         })
       } catch (uploadErr) {
         await prisma.backupHistory.update({
           where: { id: historyId },
           data: { cloudUploadStatus: "failed" },
         })
         send("cloud_upload_error", { message: (uploadErr as Error).message })
       }
     }
     ```
   - 정확한 uploadToCloud 함수명과 import 경로는 기존 코드를 확인하여 맞춘다.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>수동/스케줄 백업 모두 Connection의 backupStorageType과 backupLocalPath를 사용하여 저장 경로를 결정하고, 클라우드 타입일 때 업로드가 수행된다</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `npx prisma migrate status` — 마이그레이션 적용 완료
3. Connection 추가 모달에서 "백업 저장 설정" 섹션이 표시되는지 확인
4. 로컬/클라우드 선택에 따라 경로 입력 필드 표시/숨김 동작
5. 스케줄 모달에서 백업 저장 설정이 읽기 전용으로 표시되는지 확인
</verification>

<success_criteria>
- Connection CRUD에 backupStorageType, backupLocalPath 필드가 포함됨
- Connection 모달에서 로컬/클라우드 선택 및 로컬 경로 입력 가능
- 스케줄 모달에서 Connection의 저장 설정이 읽기 전용 표시
- 수동 백업 실행 시 Connection 설정 경로 사용
- 스케줄 백업 실행 시 Connection 설정 경로 사용
- 클라우드 타입 Connection의 백업 시 자동 클라우드 업로드
</success_criteria>

<output>
After completion, create `.planning/quick/260406-mp8-connection-connection/260406-mp8-SUMMARY.md`
</output>
